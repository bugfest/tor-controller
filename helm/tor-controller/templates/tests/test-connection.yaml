apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tor-controller.fullname" . }}-test-connection"
  labels:
    {{- include "tor-controller.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tor-controller.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
